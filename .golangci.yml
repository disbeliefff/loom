run:
  timeout: 5m

linters:
  enable-all: false
  enable:
    - errcheck
    - gosimple
    - govet
    - ineffassign
    - staticcheck
    - typecheck
    - unused

issues:
  exclude-rules:
    # Ignore errcheck for tests
    - path: _test\.go
      linters:
        - errcheck
